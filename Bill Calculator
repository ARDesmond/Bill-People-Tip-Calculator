print("Welcome to the Bill_Total calculator!")
bill = float(input("What was the total bill before tip? $"))
tip = int(input("What percentage tip would you like to give? ")) / 100
people = int(input("How many people will split the bill? "))
tip_total = bill * tip
bill_total = bill + tip_total
amount_per_person = round(bill_total / people, 2)
print(f"${amount_per_person:.2f}")
